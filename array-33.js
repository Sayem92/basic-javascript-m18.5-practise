var moduleName = ['numeric', 'string', 'boolean', 'array', 'condition', 'for loop', 'while loop', 'if', 'continue', 'break'];


for( var i=0; i < moduleName.length; i++  ){
   var moduleNameAll= moduleName[i];
    console.log(moduleNameAll);
}
